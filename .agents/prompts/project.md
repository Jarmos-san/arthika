# Project instructions

See [AGENTS.md](../../AGENTS.md) for full project conventions.

This directory exists to support the `.agents/` vendor-agnostic standard.
Add snippets, context, or package-specific overrides here as the project grows.
